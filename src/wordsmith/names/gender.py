"""Gender definitions used by name generators."""

from __future__ import annotations

from enum import Enum


class BinaryGender(Enum):
    """Binary gender values for name selection."""

    MALE = "male"
    FEMALE = "female"
